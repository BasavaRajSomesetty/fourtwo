fiat:
  admin:
    roles:
      - ninja
auth.permissions.provider.pipeline: aggregate
auth.permissions.source.pipeline.prefix:
  enabled: true
  prefixes:
   - prefix: "*"
     permissions:
       READ:
       - "ninja"
       WRITE:
       - "ninja"
       EXECUTE:
       - "ninja"
